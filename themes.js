'use strict';

const themes = new Map();

themes.set('objects', {
    '--background-color': '#dae1de',
    '--text-color': 'black',
});

themes.set('classes', {
    '--background-color': '#306b68',
    '--text-color': 'white',
});

export default themes;